version: '3.8'

services:
  api:
    build: .
    container_name: timegrave-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/timegrave.db
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
